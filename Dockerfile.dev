# Dockerfile cho Development với Hot Reload
FROM mcr.microsoft.com/dotnet/sdk:8.0
WORKDIR /app

# Copy csproj và restore dependencies
COPY ["StudentActivities.csproj", "."]
RUN dotnet restore

# Expose ports
EXPOSE 8080
EXPOSE 8081

# Cài đặt dotnet watch tool (đã có sẵn trong SDK)
# KHÔNG copy source code vào đây - sẽ mount từ host

# Run với dotnet watch để hot reload
ENTRYPOINT ["dotnet", "watch", "run", "--urls", "http://0.0.0.0:8080"]
